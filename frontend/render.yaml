services:
  - type: web
    name: prototype-creator-ui
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://prototype-creator-api.onrender.com
